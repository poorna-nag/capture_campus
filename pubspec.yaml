name: capture_campus
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  google_fonts: ^6.3.2
  path_provider: ^2.1.3
  image_picker: ^1.2.1
  equatable: ^2.0.7
  permission_handler: ^12.0.1
  geolocator: ^14.0.2
  camera: ^0.11.0
  geocoding: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/images/
